@model IEnumerable<MeuSite.Models.Entities.Despesa>
@{
    ViewData["Title"] = "Despesas";
}

<div class="container-fluid p-4">
    <div class="row mb-4">
        <div class="col">
            <h1 class="display-6 text-danger">@ViewData["Title"]</h1>
            <p class="lead">Gerencie suas despesas, investimentos e doações</p>
        </div>
        <div class="col-auto">
            <div class="btn-group" role="group">
                @if (ViewBag.ControleAnoId != null)
                {
                    <a asp-action="Create" asp-route-controleAnoId="@ViewBag.ControleAnoId" class="btn btn-danger">
                        <i class="fas fa-plus"></i> Nova Despesa
                    </a>
                }
                <a asp-controller="Controle" asp-action="Index" class="btn btn-outline-secondary">
                    <i class="fas fa-arrow-left"></i> Voltar
                </a>
            </div>
        </div>
    </div>

    <!-- Filtro por Controle Ano -->
    <div class="row mb-4">
        <div class="col-md-4">
            <div class="card">
                <div class="card-body">
                    <label class="form-label fw-bold">Filtrar por Ano:</label>
                    @{
                        var controleAnoId = ViewBag.ControleAnoId as int?;
                        var isSelected = controleAnoId?.ToString();
                    }
                    <select id="controleAnoFilter" class="form-select" onchange="location.href='@Url.Action("Index")?controleAnoId=' + this.value">
                        <option value="">Todos os anos</option>
                        @foreach (var controle in ViewBag.ControleAnos)
                        {
                            @if (isSelected == controle.Id.ToString())
                            {
                                <option value="@controle.Id" selected>@controle.Nome (@controle.Ano)</option>
                            }
                            else
                            {
                                <option value="@controle.Id">@controle.Nome (@controle.Ano)</option>
                            }
                        }
                    </select>
                </div>
            </div>
        </div>
        <div class="col-md-8">
            <div class="card bg-danger text-white">
                <div class="card-body">
                    <div class="row text-center">
                        <div class="col">
                            <h5>Total de Despesas</h5>
                            <h2>@Model.Sum(d => d.Valor):C</h2>
                        </div>
                        <div class="col">
                            <h5>Quantidade</h5>
                            <h2>@Model.Count()</h2>
                        </div>
                        <div class="col">
                            <h5>Média</h5>
                            <h2>@(Model.Any() ? Model.Average(d => d.Valor).ToString("C") : "R$ 0,00")</h2>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    @if (!Model.Any())
    {
        <div class="text-center py-5">
            <div class="mb-4">
                <i class="fas fa-minus-circle text-danger" style="font-size: 4rem;"></i>
            </div>
            <h3 class="text-muted">Nenhuma despesa cadastrada</h3>
            <p class="text-muted">Adicione sua primeira despesa para começar a controlar seus gastos</p>
            @if (ViewBag.ControleAnoId != null)
            {
                <a asp-action="Create" asp-route-controleAnoId="@ViewBag.ControleAnoId" class="btn btn-lg btn-danger">
                    <i class="fas fa-plus"></i> Adicionar Despesa
                </a>
            }
        </div>
    }
    else
    {
        <div class="card">
            <div class="card-header bg-danger text-white">
                <h5 class="mb-0">
                    <i class="fas fa-list"></i> Lista de Despesas
                    <span class="badge bg-white text-danger float-end">@Model.Count() itens</span>
                </h5>
            </div>
            <div class="card-body p-0">
                <div class="table-responsive">
                    <table class="table table-hover mb-0">
                        <thead class="table-light">
                            <tr>
                                <th>Data</th>
                                <th>Descrição</th>
                                <th>Categoria</th>
                                <th class="text-end">Valor</th>
                                <th class="text-center">Ações</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var despesa in Model)
                            {
                                <tr>
                                    <td>@despesa.Data.ToString("dd/MM/yyyy")</td>
                                    <td>
                                        <strong>@despesa.Descricao</strong>
                                        @if (!string.IsNullOrEmpty(despesa.Subcategoria?.Nome))
                                        {
                                            <br><small class="text-muted">@despesa.Subcategoria.Nome</small>
                                        }
                                    </td>
                                    <td>
                                        @if (despesa.Categoria != null)
                                        {
                                            <span class="badge" style="background-color: @(despesa.Categoria.Cor ?? "#dc3545")">
                                                @despesa.Categoria.Nome
                                            </span>
                                        }
                                        else
                                        {
                                            <span class="text-muted">Sem categoria</span>
                                        }
                                    </td>
                                    <td class="text-end">
                                        <strong class="text-danger">@despesa.Valor:C</strong>
                                    </td>
                                    <td class="text-center">
                                        <div class="btn-group" role="group">
                                            <a asp-action="Edit" asp-route-id="@despesa.Id" class="btn btn-sm btn-outline-primary">
                                                <i class="fas fa-edit"></i>
                                            </a>
                                            <a asp-action="Delete" asp-route-id="@despesa.Id" class="btn btn-sm btn-outline-danger">
                                                <i class="fas fa-trash"></i>
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                        <tfoot class="table-light">
                            <tr>
                                <th colspan="3" class="text-end">Total:</th>
                                <th class="text-end">
                                    <strong class="text-danger">@Model.Sum(d => d.Valor):C</strong>
                                </th>
                                <th></th>
                            </tr>
                        </tfoot>
                    </table>
                </div>
            </div>
        </div>
    }
</div>
